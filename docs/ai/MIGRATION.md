# 2026-09-09 协议与记忆整理记录

## 范围与权威

老板要求全工作区协议按当前任务加载，避免新对话全读、不同 Tab 漏约定。并明确移交 docs/AI-COLLABORATION.md 整理权给本次 Codex。
仅整理规则/文档/本地协作记录，不改应用功能、托管配置或他人认领。既有 AGENTS 和总协议未提交内容作为迁移基线保留。

## 去向

- AGENTS / CLAUDE / Cursor → 精简总入口、路径触发与统一 INDEX。
- 旧协议协作/Git → coordination；成本 → AGENTS；看板 → board + desires/tasks/inbox/team 四专题。
- 原 AGENTS 技术规范 + package.json 当前命令 → site；CLAUDE 原架构正文 → reference/site-architecture。
- 自动推送、本地优先、Vercel/Cloudflare 与生产 lab 隐藏 → deployment。
- 资产初衷/质量/来源/采集顺序 → assets；历史方法只定位相关章节。
- 长期规则与历史事实分层 → memory；工作史/关键决定/旧规格 → HISTORY 索引。
- 六份 docs 交接文件加历史标识，原正文保留；旧总协议完整存 reference/collaboration-2026-09-09。
- 本次爱豆产品讨论存 .ai-work/memory，本地保留，未擅自转任务或覆盖 D-001。

## 已解决的冲突

- Cursor “所有代码任务推送”补齐看板数据例外，不再常驻重复加载部署细则。
- 历史“每次读全状态/报告/规格”由按任务检索取代。
- 旧默认静态/无适配器描述不再指挥当前 Vercel 构建；build/format/lint 以现有 package.json 为准。
- “Vercel 一条404重定向足够”被当前 middleware + 静态输出清理事实替代。
- T-029 早期按任务/认领推断忙碌被最终心跳规则替代；done 不再默认当成果验收成功。

## 保留与限制

- 不删历史报告、认领、旧资产规格；原文仍可按需追溯，但不得绕过现行专题恢复旧指令。
- 未读取 Cookie/密钥或改站外全局技能；未从旧记录猜测 Cloudflare/DNS 当前账户配置。
- 本轮未改看板组件中的复制提示（已有他人认领）；目录规则已覆盖其任务。下次改提示时按所涉 Tab 补专题链接。
- 自动加载取决于客户端支持 AGENTS/Cursor 规则；协议提供明确路由，不能保证每个模型永不漏读。
- 文档任务只验证链接/规则覆盖/历史保留/认领，不运行会生成资产的整站 build；远端部署状态需另有实际证据。

## 验证结果

- 37份文档/入口/规则，71个本地文档链接可解析，8类任务可从索引进入所需协议。
- 旧协议、旧架构文档、六份交接的8份历史正文完整保留；Cursor仅1条常驻入口规则，其余按需触发。
- 欲望卡查阅链（AGENTS+索引+协作+看板+欲望）约5839字符；原AGENTS+整份协议11911字符。这里只比较字符，不等于账单 token。
- 定向规则覆盖检查与 git diff --check 通过；认领检查通过。路由检查证明文件和约定可达，不保证模型绝不漏读。
